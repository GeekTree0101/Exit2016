import { StyleSheet } from 'react-native';

const text = StyleSheet.create({

    headline: {
        color: '#efefef',
        fontSize: 50
    },

    subhead: {
        color: '#ddd',
        fontSize: 25
    },

    article: {
        color: '#e3e3e3',
        fontSize: 10
    }

})

module.exports = text;